using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickUp : MonoBehaviour
{
    //there is a inherent and annoying proble to do with picking up an item with collision
    //good for me that I'd like pickups to be interactable instead of of dealing with collision
    //the problem comes with meshing it with our existing inventory
    //though that is mostly on me i should of found a tutorial that suited the game better oopsies

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
